{
  "name": "three-rooms",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive Three Rooms Research Lab prototype with local Node and Cloudflare Worker backends.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "dev:worker": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
